{
  "name": "project-its-vehicle-rental",
  "version": "0.1.0",
  "private": true,
  "description": "Vehicle rental web application — Next.js + Python Flask",
  "scripts": {
    "dev:client": "npm run dev --prefix client",
    "build:client": "npm run build --prefix client",
    "install:all": "npm install && npm install --prefix client"
  },
  "keywords": ["vehicle", "rental", "web"],
  "author": "",
  "license": "UNLICENSED"
}
